#include<bits/stdc++.h>
using namespace std;

int main(){
    float a=2.3;
    float b=4.5;

    if(a>b){
        cout<<"the greatest of two no is "<<a<<endl;
    }
    else {
       cout<<"the greatest of two no is "<<b<<endl; 
    }
    return 0;
}